/**
 * @deprecated ERC20Deposited stub — 统一国库入金已并入 BUnitPurchased（`startBaseVoteListen`）。
 * 跨链铸币改由 `startPeerBridgeVoteListen`（ConetTreasuryPeer BridgeOut → voteMintFromPeer*）。
 */
export { startPeerBridgeVoteListen } from './peerBridgeListen'
